x = (double)rand() / RAND_MAX; //実数の乱数を生成するために変数の型を変換（キャスト）する
上記の変換では、rans()のみが整数から倍精度実数に変換される。
式の中で異なる型の変数が現れた時は精度の高い型に合わせて演算が行われる。

円周率はmath.hの M_PIを利用することで、利用できる。
絶対値計算はfabs()を使えばいい
平方根はsqrt()
abs reffers to absolute

標準出力をファイルに保存するにはディライレクトを使う。
./k1-1_8120054.o > k1-1_8120054.txt
